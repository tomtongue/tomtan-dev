---
const today = new Date();
---

<footer class="footer">
    <div class="footer-content">
        <div class="footer-copyright">
            &copy; {today.getFullYear()} Tomohiro Tanaka. All rights reserved.
        </div>
    </div>
</footer>

<style>
/* フッターのベーススタイル */
.footer {
    --footer-background: #f5f5f7;
    --footer-text: #86868b;
    --footer-border: #d2d2d7;
    --footer-height: 4rem;
    --footer-padding: 2rem;

    width: 100%;
    background-color: var(--footer-background);
    padding: var(--footer-padding) 0;
    border-top: 1px solid var(--footer-border);
    
    /* フッターを下部に固定するための設定 */
    margin-top: auto;
    flex-shrink: 0;
}

/* フッターコンテンツのレイアウト */
.footer-content {
    max-width: var(--content-width, 75rem);
    margin: 0 auto;
    padding: 0 var(--content-padding, 2rem);
    display: flex;
    justify-content: center;
    align-items: center;
}

/* コピーライトテキストのスタイル */
.footer-copyright {
    font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", "Helvetica Neue", sans-serif;
    font-size: 0.875rem;
    color: var(--footer-text);
    text-align: center;
    line-height: 1.4;
}

/* レスポンシブ対応 */
@media (max-width: 768px) {
    .footer {
        --footer-padding: 1.5rem;
    }

    .footer-content {
        flex-direction: column;
        gap: 1rem;
    }
}

/* ダークモード対応 */
@media (prefers-color-scheme: dark) {
    .footer {
        --footer-background: #1d1d1f;
        --footer-text: #86868b;
        --footer-border: #424245;
    }
}

/* 印刷用スタイル */
@media print {
    .footer {
        display: none;
    }
}

/* グローバルスタイルの追加（親要素に適用する必要があります） */
:global(html) {
    height: 100%;
}

:global(body) {
    min-height: 100%;
    display: flex;
    flex-direction: column;
}

:global(body > div) {
    flex: 1 0 auto;
    display: flex;
    flex-direction: column;
}
</style>